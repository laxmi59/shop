<% if @avatar_show.attached? %>
  <%#= image_tag @avatar_show.variant(resize_to_limit: [200, 200]) %>
  <%= image_tag(@avatar_show, size:"200x200")%>
<% else %>
  <%= image_tag("no_image.png", size:"200x200")%>
<% end %>
